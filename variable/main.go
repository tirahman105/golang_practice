package main

import "fmt"

func main() {
	var x string
	// this is an example of variable declaration.
	// It creats a new variable  It creates a new variable with a name (x) and a type (string).

	x = "This is Golang"

	//  We then assign a value to that variable (Hello World)
	fmt.Println(x)

	//  and then retrieve the value when we send it to fmt.Println.
}
